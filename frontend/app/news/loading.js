export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
      <div className="flex text-white bg-black min-h-screen min-w-screen items-center justify-center">
        loading...
      </div>
    );
  }
  